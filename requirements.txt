# Python-side dependencies for the Telegram bridge and helpers
python-telegram-bot>=21.0
python-dotenv>=1.0.0
mss>=9.0.0
playwright>=1.40.0
# After install, run: py -3.11 -m playwright install chromium
# Claude execution is provided by the locally installed Claude Code CLI (`claude`).
